import { formatUzs,usdToSellingUzs } from "@/lib/pricing/money";
export function ProductPrice({priceUsd,exchangeRate,compact=false}:{priceUsd?:string|null;exchangeRate:string|null;compact?:boolean}){const uzs=priceUsd&&exchangeRate?usdToSellingUzs(priceUsd,exchangeRate):null;if(uzs===null)return <span className={`product-price-request${compact?" is-compact":""}`}>Narxni so‘rash</span>;return <div className={`product-price${compact?" is-compact":""}`}><strong>{formatUzs(uzs)}</strong><small>CBU kursi bo‘yicha</small></div>}
