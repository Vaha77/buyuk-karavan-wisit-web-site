"use client";
import { useEffect, useState } from "react";
import { ArrowRight, Camera as Instagram, Menu, Send, X } from "lucide-react";
const links = [["Bosh sahifa","#top"],["Mahsulotlar","#mahsulotlar"],["Sovutish kameralari","#yechimlar"],["Loyihalar","#loyihalar"],["Biz haqimizda","#biz-haqimizda"],["Aloqa","#aloqa"]] as const;
function Brand({ light = false }: { light?: boolean }) { return <span className={`brand ${light?"light":""}`}><span className="brand-mark" aria-hidden="true">✳</span> BUYUK KARAVAN</span>; }
export function Header() { const [open,setOpen]=useState(false); useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]); useEffect(()=>{const onKey=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)};window.addEventListener("keydown",onKey);return()=>window.removeEventListener("keydown",onKey)},[]); return <><header className="site-header" id="top"><div className="header-inner"><a href="#top" aria-label="BUYUK KARAVAN — bosh sahifa"><Brand/></a><nav className="desktop-nav" aria-label="Asosiy navigatsiya">{links.map(([label,href])=><a key={label} href={href}>{label}</a>)}</nav><a className="button button-blue header-cta" href="#aloqa">Bepul hisob-kitob <ArrowRight size={16}/></a><button className="menu-toggle" type="button" onClick={()=>setOpen(true)} aria-label="Menyuni ochish" aria-expanded={open}><Menu size={23}/></button></div></header><div className={`mobile-menu ${open?"is-open":""}`} aria-hidden={!open}><div className="mobile-menu-top"><a href="#top" onClick={()=>setOpen(false)}><Brand light/></a><button type="button" onClick={()=>setOpen(false)} aria-label="Menyuni yopish"><X size={29}/></button></div><nav aria-label="Mobil navigatsiya">{links.map(([label,href])=><a href={href} key={label} onClick={()=>setOpen(false)} tabIndex={open?0:-1}>{label}<ArrowRight size={17}/></a>)}</nav><div className="mobile-menu-bottom"><a href="#aloqa" className="button button-white" onClick={()=>setOpen(false)} tabIndex={open?0:-1}>Bepul hisob-kitob <ArrowRight size={17}/></a><div className="socials"><a href="https://instagram.com/buyuk_karavan" aria-label="Instagram" tabIndex={open?0:-1}><Instagram size={18}/></a><a href="https://t.me/buyuk_karavan" aria-label="Telegram" tabIndex={open?0:-1}><Send size={18}/></a></div></div></div></>; }
