"use client";
import { useState } from "react";
import { ArrowRight, BriefcaseBusiness, Flame, IceCreamCone, Lightbulb, Moon, Snowflake } from "lucide-react";
const icons={fruit:Flame,moon:Moon,meat:Lightbulb,ice:IceCreamCone,snow:Snowflake,case:BriefcaseBusiness};
type SelectorContent={eyebrow:string;title:string;description:string;items:{id:string;title:string;icon:string;isVisible:boolean;order:number}[]};
export function Selector({content}:{content:SelectorContent}){const items=content.items.filter(item=>item.isVisible).sort((a,b)=>a.order-b.order);const[selected,setSelected]=useState<string>(items[0]?.id??"");return <section className="selector-section" id="tanlov"><div className="container"><div className="selector-intro"><p className="eyebrow">{content.eyebrow}</p><h2>{content.title}</h2><p>{content.description}</p></div><div className="type-grid" role="group" aria-label="Mahsulot turi">{items.map(item=>{const Icon=icons[item.icon as keyof typeof icons]??BriefcaseBusiness;return <button className={`type-card ${selected===item.id?"selected":""}`} key={item.id} type="button" aria-pressed={selected===item.id} onClick={()=>setSelected(item.id)}><span className="type-icon"><Icon size={22} strokeWidth={1.8}/></span><span>{item.title}</span></button>})}</div><div className="selector-steps"><div className="step"><b>1</b><span>Harorat</span></div><ArrowRight className="step-arrow" size={24} strokeWidth={1}/><div className="step"><b>2</b><span>Xona hajmi</span></div><ArrowRight className="step-arrow" size={24} strokeWidth={1}/><div className="step"><b>3</b><span>Tavsiya etilgan yechim</span></div><a href="#aloqa" className="button button-blue">Hisob-kitobni boshlash</a></div></div></section>}
