"use client";
import { useState,useTransition } from "react";import { useRouter } from "next/navigation";import { approveSalesAgentAction,toggleSalesAgentAction } from "@/app/admin/(protected)/sales-agents/actions";import type { AdminSalesAgent } from "@/lib/sales-agents/queries";
export function AdminSalesAgents({agents}:{agents:AdminSalesAgent[]}){const [feedback,setFeedback]=useState(""),[pending,start]=useTransition();const router=useRouter();const run=(action:(id:string)=>Promise<{error?:string}>,id:string,message:string)=>start(async()=>{const result=await action(id);setFeedback(result.error||message);router.refresh()});return <div className="admin-products-page" aria-busy={pending}><div className="admin-page-heading"><div><h1>Sotuvchilar</h1><p>BKLead Telegram sotuvchilarini tasdiqlash va boshqarish</p></div></div>{feedback&&<p className="admin-form-feedback" role="status">{feedback}</p>}<section className="admin-panel admin-management-panel"><div className="admin-panel-heading"><h2>Ro‘yxatdan o‘tgan sotuvchilar</h2><span>{agents.length} ta</span></div>{agents.length?<div className="admin-table-wrap"><table className="admin-products-table"><thead><tr><th>Ism</th><th>Telegram</th><th>Ro‘yxatdan o‘tgan</th><th>Tasdiq</th><th>Faollik</th><th>Amal</th></tr></thead><tbody>{agents.map(agent=><tr key={agent.id}><td><strong>{agent.name}</strong></td><td>{agent.username}</td><td>{agent.createdAt}</td><td>{agent.isApproved?"Tasdiqlangan":"Kutilmoqda"}</td><td>{agent.isActive?"Faol":"Faol emas"}</td><td><div className="admin-row-tools">{!agent.isApproved&&<button onClick={()=>run(approveSalesAgentAction,agent.id,"Sotuvchi tasdiqlandi.")}>Tasdiqlash</button>}<button onClick={()=>run(toggleSalesAgentAction,agent.id,agent.isActive?"Sotuvchi faolsizlantirildi.":"Sotuvchi faollashtirildi.")}>{agent.isActive?"Faolsizlantirish":"Faollashtirish"}</button></div></td></tr>)}</tbody></table></div>:<p className="admin-empty">Hozircha sotuvchilar ro‘yxatdan o‘tmagan.</p>}</section></div>}
