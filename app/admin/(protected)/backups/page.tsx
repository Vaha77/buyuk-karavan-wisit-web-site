import Link from "next/link";import {requireAdmin} from "@/lib/auth/require-admin";import {getDb} from "@/lib/db";import {createBackupAction} from "./actions";
export default async function BackupsPage(){await requireAdmin();const runs=await getDb().backupRun.findMany({orderBy:{startedAt:"desc"},take:30});return <div><div className="admin-page-heading"><div><h1>CRM Backup</h1><p>Excel eksport va timestamp bilan doimiy nusxalar</p></div><Link className="admin-primary-button" href="/admin/backups/download">CRM Excel yuklab olish</Link></div><section className="admin-panel admin-management-panel"><form action={createBackupAction}><button className="admin-primary-button">Doimiy backup yaratish</button></form><div className="admin-table-wrap"><table className="admin-products-table"><thead><tr><th>Sana</th><th>Holat</th><th>Fayl</th><th>Hajm</th></tr></thead><tbody>{runs.map(r=><tr key={r.id}><td>{r.startedAt.toISOString()}</td><td>{r.status}</td><td>{r.storageKey||r.errorType||"—"}</td><td>{r.sizeBytes||"—"}</td></tr>)}</tbody></table></div></section></div>}
