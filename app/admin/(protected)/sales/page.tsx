import {getDb} from "@/lib/db";import {requireAdmin} from "@/lib/auth/require-admin";import {approveSaleAction,rejectSaleAction} from "./actions";
export default async function SalesPage(){await requireAdmin();const sales=await getDb().sale.findMany({orderBy:{reportedAt:"desc"},include:{agent:true,lead:true}});return <div className="admin-products-page"><div className="admin-page-heading"><div><h1>Sotuvlar</h1><p>Sotuvchilar yuborgan savdolarni tasdiqlash</p></div></div><section className="admin-panel admin-management-panel"><div className="admin-table-wrap"><table className="admin-products-table"><thead><tr><th>Mijoz</th><th>Sotuvchi</th><th>Sotilgan mahsulot</th><th>Sana</th><th>Holat</th><th>Amal</th></tr></thead><tbody>{sales.map(s=><tr key={s.id}><td><strong>{s.customerName||"Ko‘rsatilmagan"}</strong></td><td>{[s.agent.firstName,s.agent.lastName].filter(Boolean).join(" ")}</td><td>{s.productDescription}</td><td>{new Intl.DateTimeFormat("uz-UZ",{timeZone:"Asia/Tashkent",dateStyle:"medium",timeStyle:"short"}).format(s.reportedAt)}</td><td><span className={`sales-agent-status ${s.status==="APPROVED"?"is-active":s.status==="REJECTED"?"is-inactive":""}`}>{s.status}</span></td><td>{s.status==="PENDING"&&<div className="admin-row-tools sales-agent-actions"><form action={approveSaleAction}><input type="hidden" name="id" value={s.id}/><button className="sales-agent-button is-primary">Tasdiqlash</button></form><form action={rejectSaleAction}><input type="hidden" name="id" value={s.id}/><input name="reason" required minLength={3} maxLength={500} placeholder="Rad etish sababi"/><button className="sales-agent-button is-danger">Rad etish</button></form></div>}</td></tr>)}</tbody></table></div></section></div>}
